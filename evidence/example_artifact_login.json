{
  "capability_id": "login",
  "version": 1,
  "description": "Log in to ParaBank so an authenticated session exists for other capabilities to run in.",
  "target": {
    "base_url": "https://parabank.parasoft.com/parabank/index.htm",
    "description": "Log in to ParaBank so an authenticated session exists for other capabilities to run in."
  },
  "inputs": [
    {
      "name": "username",
      "type": "string",
      "required": true,
      "description": "ParaBank login username"
    },
    {
      "name": "password",
      "type": "string",
      "required": true,
      "description": "ParaBank login password"
    }
  ],
  "outputs": [],
  "steps": [
    {
      "index": 1,
      "action": "fill",
      "locator": {
        "strategy": "css",
        "value": "input[name=\"username\"]",
        "fallback": null,
        "reasoning": "The username input has name attribute but no id, so use css selector by name."
      },
      "value": "{username}",
      "description": "The username input has name attribute but no id, so use css selector by name."
    },
    {
      "index": 2,
      "action": "fill",
      "locator": {
        "strategy": "css",
        "value": "input[name=\"password\"]",
        "fallback": null,
        "reasoning": "The password input has name attribute but no id, so use css selector by name."
      },
      "value": "{password}",
      "description": "The password input has name attribute but no id, so use css selector by name."
    },
    {
      "index": 3,
      "action": "click",
      "locator": {
        "strategy": "css",
        "value": "input[type=\"submit\"]",
        "fallback": null,
        "reasoning": "Submit button to log in."
      },
      "value": null,
      "description": "Submit button to log in."
    }
  ],
  "checkpoint": [
    {
      "kind": "text_present",
      "text": "Accounts Overview",
      "locator": null,
      "pattern": null,
      "description": "Heading indicating successful login and account services area reached. Manually dropped a second, unsatisfiable element_visible condition with no locator that discovery self-reported - see project memory."
    }
  ],
  "known_outcomes": [],
  "created_from_run": "verify_login_0003"
}